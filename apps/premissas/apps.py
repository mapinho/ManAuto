from django.apps import AppConfig


class PremissasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.premissas"
    verbose_name = "Premissas versionadas e cenarios"
