# Gerado a partir de pyproject.toml (fonte da verdade — SPEC_Tecnica_Ambiente.md
# decide uv como gestor de dependencias). Este arquivo e uma conveniencia para
# `pip install -r requirements.txt` em ambientes sem uv (ex.: Windows local, sem
# Docker) — se pyproject.toml mudar, atualize este arquivo junto.
python-dotenv>=1.0
django>=6.0,<7.0
djangorestframework>=3.15
procrastinate[django]>=2.9
psycopg[binary]>=3.1
openpyxl>=3.1
python-pptx>=1.0
pandas>=2.2
django-simple-history>=3.5
django-htmx>=1.17
whitenoise>=6.6
gunicorn>=22.0
argon2-cffi>=23.1
